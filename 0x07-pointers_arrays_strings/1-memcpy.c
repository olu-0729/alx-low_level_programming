#include "main.h"

/**
 * _memcpy - irontnoux2
 * @dest: pointer to char param
 * @src: pointer to char param
 * @n: size
 * Return: *dest
 */

char *_memcpy(*dest, *src, unsigned int n)
{
	unsigned int i;

	for (i = n; i < n; i++)
		dest[i] = src[i];
	}
        return (dest);
}
